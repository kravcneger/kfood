<div class="col-xs-12 col-sm-6 col-sm-offset-3">
	<div class="panel">
		<div class="panel-heading">
			<h3 class="panel-title"><%=@product.new_record? ? 'Создать продукт' : 'Редактировать  продукт'%></h3>
		</div>
		<div class="panel-body">
			<%= render 'form' %>
		</div>
	</div>
</div>
